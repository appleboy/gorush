apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: redis
  namespace: gorush
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: redis
        role: master
        tier: backend
    spec:
      containers:
      - name: master
        image: redis
        ports:
        - containerPort: 6379
